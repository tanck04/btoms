package controller;

import enums.FlatType;
import model.Project;
import model.User;

import java.util.List;

public interface ViewProjectInterface {
    // View project method already exists
    void viewProject(User user);
}
